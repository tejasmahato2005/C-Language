#include <stdio.h>

int main (){
    int r,h;
    r = 6;
    h = 156;
    printf("the area of the circle with radius %d is %f\n", r , 3.14*r*r);
    printf("the volume of a cylinder with radius %d and height %d is %f\n",r, h,  3.14*r*r*h);
}